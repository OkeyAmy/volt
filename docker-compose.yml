version: "3.9"

services:
  volt-api:
    build: .
    container_name: volt-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - GEMINI_MODEL=${GEMINI_MODEL:-gemini-3.5-flash}
    restart: unless-stopped
